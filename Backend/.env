# Default language for translation (en, es, fr, de, etc.)
DEFAULT_LANGUAGE=en

# Google Cloud Translate API (Optional - for production)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/credentials.json

# Flask configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=5000

# CORS configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging level
LOG_LEVEL=INFO